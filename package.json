{
  "name": "login_register",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "frontend": "cd frontend && yarn && yarn dev",
    "backend": "cd backend && yarn && yarn dev",
    "database": "cd database && yarn && yarn db",
    "dev": "yarn database & yarn backend & yarn frontend",
    "clear": "rm -rf database/node_modules backend/node_modules frontend/node_modules",
    "secret": "cd utils && yarn gen_secret"
  }
}